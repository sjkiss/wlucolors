# This function defines the full palette of colors in this package

create_palette<-function(x) {

  my_colors <<- c(
  `liberal`        = "darkred",
  `conservative`      = "darkblue",
  `ndp`       = "orange",
  `bq` = "cyan",
  `green`     = "darkgreen",
  `laurier1`     = "darkorchid",
  `laurier2` = "gold")


}
#This actually runs the function to make the vector my_colors available to the global environment
#create_palette()



